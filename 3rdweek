변수 선언 (const, let, var)
const: 값을 바꿀 수 없음
let: 코드 중간에 값 변경 가능
var: 변경이 가능하지만 자바스크립트 내에서 변수가 변경될 수 있는지의 여부를 알려주지 않는다

배열을 선언할 때도 똑같이 변수 선언 명령어 사용
ex) const array = [1, 2, 3, 4]
배열에 원소를 삽입하고 싶을 때는 .push()를 배열 뒤에 쓴 후 괄호 안에 원하는 원소 추가

object (구조체 같은 개념인 듯?)
선언방식 ex)
const player = {name: "Noah",
points: 
};
구조체의 원소에 접근하고 싶을 때는 c언어와 동일하게 player.name 같은 방식으로 해준다.
player["name"]같은 방식도 되는 듯
object 안의 원소 변경은 가능(구조체의 틀만 유지하고 있다면) ex) object를 문자열로 수정한다든가의 일은 불가능
object에 새로운 원소를 넣고 싶다면 그냥 선언하면 된다.
